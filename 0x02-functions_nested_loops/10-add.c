#include "main.h"

/**
 * add - Adds two integers and prints the result
 * @a: int to add
 * @b: int to add
 * Return: the sum of integers
 */
int add(int a, int b)
{
int x;

x = a + b;
return (x);
}
